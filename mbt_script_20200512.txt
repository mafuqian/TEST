update gp_qc_rule set MIN_LENGTH=2,MAX_LENGTH=2 where DATA_ID = 'MBT_210__C_FIRST_HOU_LOAN_FLAG';
update gp_qc_rule set nullable='Y',script='if ((!isEmpty(IS_RPT) && IS_RPT.substring(4,5) == "1") && (B_ACCT_CODE == "R2")) {
    if (isEmpty($value)) {
        return false
    }
}
return true' where data_id = 'MBT_210__H_NOTLSU_BAL';